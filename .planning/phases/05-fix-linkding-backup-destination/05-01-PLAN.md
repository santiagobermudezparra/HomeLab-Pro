---
phase: 05-fix-linkding-backup-destination
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - databases/staging/linkding/postgresql-cluster.yaml
  - databases/staging/linkding/kustomization.yaml
  - databases/staging/linkding/linkding-backup-s3-secret.yaml
  - databases/staging/n8n/postgresql-cluster.yaml
  - databases/staging/n8n/kustomization.yaml
  - databases/staging/n8n/n8n-backup-s3-secret.yaml
autonomous: false
requirements:
  - BACK-02
must_haves:
  truths:
    - "linkding-postgres Cluster has spec.backup.barmanObjectStore with a destinationPath pointing to an S3-compatible bucket"
    - "A SOPS-encrypted linkding-backup-s3-secret Secret exists in the linkding namespace with ACCESS_KEY_ID and ACCESS_KEY_SECRET"
    - "backup-config.yaml is uncommented and active in databases/staging/linkding/kustomization.yaml"
    - "kubectl kustomize databases/staging/linkding/ succeeds and output contains both ScheduledBackup and barmanObjectStore"
    - "At least one Backup object in the linkding namespace shows phase: completed after FluxCD sync"
    - "n8n-postgresql-cluster Cluster has spec.backup.barmanObjectStore with destinationPath pointing to s3://homelab-postgres-backups/n8n"
    - "A SOPS-encrypted n8n-backup-s3-secret Secret exists in the n8n namespace"
    - "At least one Backup object in the n8n namespace shows phase: completed after FluxCD sync"
  artifacts:
    - path: "databases/staging/linkding/linkding-backup-s3-secret.yaml"
      provides: "SOPS-encrypted S3 credentials for linkding barman-cloud-backup"
      contains: "ENC[AES256_GCM"
    - path: "databases/staging/linkding/postgresql-cluster.yaml"
      provides: "linkding-postgres Cluster with barmanObjectStore backup config"
      contains: "barmanObjectStore"
    - path: "databases/staging/linkding/kustomization.yaml"
      provides: "Active kustomization including both backup-config.yaml and the new secret"
      contains: "- backup-config.yaml"
    - path: "databases/staging/n8n/n8n-backup-s3-secret.yaml"
      provides: "SOPS-encrypted S3 credentials for n8n barman-cloud-backup"
      contains: "ENC[AES256_GCM"
    - path: "databases/staging/n8n/postgresql-cluster.yaml"
      provides: "n8n-postgresql-cluster Cluster with barmanObjectStore backup config"
      contains: "barmanObjectStore"
    - path: "databases/staging/n8n/kustomization.yaml"
      provides: "Active kustomization including n8n-backup-s3-secret.yaml"
      contains: "- n8n-backup-s3-secret.yaml"
  key_links:
    - from: "databases/staging/linkding/postgresql-cluster.yaml"
      to: "databases/staging/linkding/linkding-backup-s3-secret.yaml"
      via: "spec.backup.barmanObjectStore.s3Credentials.accessKeyId.name"
      pattern: "name: linkding-backup-s3-secret"
    - from: "databases/staging/linkding/kustomization.yaml"
      to: "databases/staging/linkding/backup-config.yaml"
      via: "resources list entry"
      pattern: "- backup-config.yaml"
    - from: "databases/staging/linkding/kustomization.yaml"
      to: "databases/staging/linkding/linkding-backup-s3-secret.yaml"
      via: "resources list entry"
      pattern: "- linkding-backup-s3-secret.yaml"
    - from: "databases/staging/n8n/postgresql-cluster.yaml"
      to: "databases/staging/n8n/n8n-backup-s3-secret.yaml"
      via: "spec.backup.barmanObjectStore.s3Credentials.accessKeyId.name"
      pattern: "name: n8n-backup-s3-secret"
    - from: "databases/staging/n8n/kustomization.yaml"
      to: "databases/staging/n8n/n8n-backup-s3-secret.yaml"
      via: "resources list entry"
      pattern: "- n8n-backup-s3-secret.yaml"
---

<objective>
Wire the existing ScheduledBackups for BOTH linkding and n8n to a Cloudflare R2 object store
by adding the missing barmanObjectStore stanza to each Cluster resource, creating SOPS-encrypted
S3 credential secrets, and activating the kustomizations.

Purpose: Satisfies BACK-02 — both linkding and n8n have ScheduledBackup CRDs but fail with
"no barmanObjectStore section defined" because neither Cluster has a backup destination configured.
This plan adds the destination and credentials for both so backups persist to Cloudflare R2.

Credentials are available in .env (not committed — added to .gitignore):
  BUCKET_NAME=homelab-postgres-backups
  ACCOUNT_ID=cf504e28de7836d9611b6774cdcb303e
  ACCESS_KEY_ID and ACCESS_KEY_SECRET are also in .env

Both clusters share the same R2 bucket with separate path prefixes:
  linkding → s3://homelab-postgres-backups/linkding
  n8n      → s3://homelab-postgres-backups/n8n

Output (linkding):
- databases/staging/linkding/linkding-backup-s3-secret.yaml (new — SOPS-encrypted S3 credentials)
- databases/staging/linkding/postgresql-cluster.yaml (modified — add spec.backup.barmanObjectStore)
- databases/staging/linkding/kustomization.yaml (modified — uncomment backup-config.yaml, add secret)

Output (n8n):
- databases/staging/n8n/n8n-backup-s3-secret.yaml (new — SOPS-encrypted S3 credentials)
- databases/staging/n8n/postgresql-cluster.yaml (modified — add spec.backup.barmanObjectStore)
- databases/staging/n8n/kustomization.yaml (modified — add n8n-backup-s3-secret.yaml resource)

NOTE: n8n/kustomization.yaml already includes backup-config.yaml (no need to uncomment).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

Research (read in full — contains barmanObjectStore spec, pitfall list, validation commands):
@.planning/phases/05-fix-linkding-backup-destination/05-RESEARCH.md

Reference pattern (established SOPS encryption format for linkding):
@databases/staging/linkding/secrets.yaml
</context>

<interfaces>
<!-- Key contracts the executor needs. No codebase exploration required. -->

From databases/staging/linkding/postgresql-cluster.yaml (current state — must preserve all
existing fields and ADD spec.backup below spec.bootstrap):
```yaml
apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: linkding-postgres
  namespace: linkding
spec:
  description: "PostgreSQL cluster for Linkding application"
  instances: 1
  postgresql:
    parameters:
      max_connections: "100"
      shared_buffers: "128MB"
      effective_cache_size: "512MB"
  storage:
    size: 2Gi
  superuserSecret:
    name: linkding-superuser
  bootstrap:
    initdb:
      database: linkding
      owner: linkding
      secret:
        name: linkding-db-credentials
  # ADD spec.backup block here — see Task 2
```

From databases/staging/linkding/kustomization.yaml (current state — note backup-config.yaml
is commented out; both lines must be activated):
```yaml
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: linkding
resources:
  - secrets.yaml
  - postgresql-cluster.yaml
  # - backup-config.yaml     ← UNCOMMENT
                              # ADD: - linkding-backup-s3-secret.yaml
```

From databases/staging/linkding/backup-config.yaml (NO CHANGES NEEDED — already correct):
```yaml
apiVersion: postgresql.cnpg.io/v1
kind: ScheduledBackup
metadata:
  name: linkding-backup
  namespace: linkding
spec:
  schedule: "0 3 * * *"
  immediate: true
  cluster:
    name: linkding-postgres
  backupOwnerReference: cluster
```

From databases/staging/n8n/postgresql-cluster.yaml (current state — must preserve all existing
fields and ADD spec.backup below spec.bootstrap):
```yaml
apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: n8n-postgresql-cluster
  namespace: n8n
spec:
  description: "PostgreSQL cluster for n8n application"
  instances: 1
  postgresql:
    parameters:
      max_connections: "200"
      shared_buffers: "128MB"
      effective_cache_size: "512MB"
  storage:
    size: 2Gi
  superuserSecret:
    name: n8n-superuser
  bootstrap:
    initdb:
      database: n8n
      owner: n8n
      secret:
        name: n8n-db-credentials
  # ADD spec.backup block here — see Task 4
```

From databases/staging/n8n/kustomization.yaml (current state — backup-config.yaml is ALREADY
active; only need to add the secret reference):
```yaml
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - secrets.yaml
  - postgresql-cluster.yaml
  - backup-config.yaml
                   # ADD: - n8n-backup-s3-secret.yaml
```

NOTE: For deploying new apps to the homelab (not database migrations), use the
homelab-app-onboarding skill at `.claude/skills/homelab-app-onboarding/`. It handles
creating Kubernetes manifests, encrypting secrets with SOPS, and opening a PR automatically.

SOPS age recipient (from clusters/staging/.sops.yaml):
age1spwc8lctzldd0ghkkls8jfvzzra7cx95r2zqq6eya84etq65wfgqy2h99p

barmanObjectStore spec (note: use ACCESS_KEY_SECRET for the secretAccessKey key name — matches
secret shape defined in Task 1):
```yaml
  backup:
    barmanObjectStore:
      destinationPath: "s3://<BUCKET_NAME>/linkding"
      endpointURL: "https://<ACCOUNT_ID>.r2.cloudflarestorage.com"
      s3Credentials:
        accessKeyId:
          name: linkding-backup-s3-secret
          key: ACCESS_KEY_ID
        secretAccessKey:
          name: linkding-backup-s3-secret
          key: ACCESS_KEY_SECRET
      wal:
        compression: gzip
      data:
        compression: gzip
    retentionPolicy: "30d"
```
</interfaces>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Provide S3/R2 credentials and bucket details</name>
  <files>none</files>
  <action>
The executor needs S3-compatible credentials before any file changes can be made. Claude cannot
guess these values. The user must gather the following from their chosen object storage provider
(Cloudflare R2 is recommended — free egress, no storage class complexity, compatible with
barman-cloud-backup).

**For Cloudflare R2:**
1. Log in to Cloudflare Dashboard → R2 → Overview
2. Create a bucket named (suggestion: `homelab-postgres-backups` or any name you choose)
3. Note your Account ID (shown on the R2 Overview page — format: 32-char hex string)
4. Go to R2 → Manage R2 API Tokens → Create API Token
   - Permissions: Object Read and Write
   - Scope: Specific bucket → (your bucket name) OR All buckets
   - Click Create API Token
5. Note: Access Key ID and Secret Access Key (shown once — copy both immediately)

**Provide the following four values:**
- BUCKET_NAME: (e.g., `homelab-postgres-backups`)
- ACCOUNT_ID: (e.g., `abc123def456abc123def456abc123de`)
- ACCESS_KEY_ID: (the R2 API token access key)
- ACCESS_KEY_SECRET: (the R2 API token secret key)

**If using a different S3-compatible provider (Backblaze B2, AWS S3):**
- BUCKET_NAME: your bucket name
- ENDPOINT_URL: provider-specific endpoint (e.g., `https://s3.us-east-1.amazonaws.com`)
- ACCESS_KEY_ID: your access key
- ACCESS_KEY_SECRET: your secret key
Note: Do NOT use Backblaze B2 with CNPG 1.24 — known HeadBucket failures (issues #7105/#8415).
  </action>
  <verify>
    <automated>MISSING — credentials come from user; no automated check possible before they are provided</automated>
  </verify>
  <done>
    User has provided: BUCKET_NAME, ACCOUNT_ID (or full ENDPOINT_URL), ACCESS_KEY_ID, ACCESS_KEY_SECRET.
    The S3 bucket EXISTS in the provider dashboard before proceeding (CNPG does not create the bucket).
  </done>
  <resume-signal>
Provide the four values above (BUCKET_NAME, ACCOUNT_ID or ENDPOINT_URL, ACCESS_KEY_ID,
ACCESS_KEY_SECRET) and confirm the bucket has been created. Type "ready" followed by the values,
e.g.:
  ready
  BUCKET_NAME=homelab-postgres-backups
  ACCOUNT_ID=abc123...
  ACCESS_KEY_ID=...
  ACCESS_KEY_SECRET=...
  </resume-signal>
</task>

<task type="auto">
  <name>Task 2: Create SOPS-encrypted S3 credentials secret</name>
  <files>databases/staging/linkding/linkding-backup-s3-secret.yaml</files>

  <read_first>
    - databases/staging/linkding/secrets.yaml (reference SOPS format — must match exactly)
    - clusters/staging/.sops.yaml (confirm age recipient before encrypting)
  </read_first>

  <action>
Using the credentials provided in Task 1, create and encrypt the S3 credentials secret.

**Step 1 — Create plaintext secret file**

Write databases/staging/linkding/linkding-backup-s3-secret.yaml with the following content,
substituting the user-provided ACCESS_KEY_ID and ACCESS_KEY_SECRET values:

```yaml
apiVersion: v1
kind: Secret
metadata:
  name: linkding-backup-s3-secret
  namespace: linkding
type: Opaque
stringData:
  ACCESS_KEY_ID: <ACCESS_KEY_ID from Task 1>
  ACCESS_KEY_SECRET: <ACCESS_KEY_SECRET from Task 1>
```

**Step 2 — Verify the plaintext file before encrypting**

Run: `cat databases/staging/linkding/linkding-backup-s3-secret.yaml`

Confirm:
- `name: linkding-backup-s3-secret` is present
- `namespace: linkding` is present
- Both `ACCESS_KEY_ID` and `ACCESS_KEY_SECRET` have the user-provided values (not placeholders)
- `type: Opaque` is present

Do NOT proceed to Step 3 until the file looks correct.

**Step 3 — Encrypt in place with SOPS**

```bash
sops --age=age1spwc8lctzldd0ghkkls8jfvzzra7cx95r2zqq6eya84etq65wfgqy2h99p \
  --encrypt \
  --encrypted-regex '^(data|stringData)$' \
  --in-place databases/staging/linkding/linkding-backup-s3-secret.yaml
```

**Step 4 — Verify encryption succeeded**

Run: `grep 'ENC\[AES256_GCM' databases/staging/linkding/linkding-backup-s3-secret.yaml`

This must return two matches (one for ACCESS_KEY_ID, one for ACCESS_KEY_SECRET). If it returns
zero matches, stop and re-run the sops encrypt command.

CRITICAL: Never commit this file if it still contains plaintext credential values. The SOPS
`sops:` block at the bottom of the file and `ENC[AES256_GCM` values in stringData are the
confirmation that encryption succeeded.
  </action>

  <verify>
    <automated>grep -c 'ENC\[AES256_GCM' databases/staging/linkding/linkding-backup-s3-secret.yaml</automated>
  </verify>

  <acceptance_criteria>
    - File exists: `ls databases/staging/linkding/linkding-backup-s3-secret.yaml` exits 0
    - File is encrypted: `grep -c 'ENC\[AES256_GCM' databases/staging/linkding/linkding-backup-s3-secret.yaml` returns 2 (both stringData fields encrypted)
    - File has correct name: `grep 'name: linkding-backup-s3-secret' databases/staging/linkding/linkding-backup-s3-secret.yaml` returns a match
    - File has correct namespace: `grep 'namespace: linkding' databases/staging/linkding/linkding-backup-s3-secret.yaml` returns a match
    - File has correct type: `grep 'type: Opaque' databases/staging/linkding/linkding-backup-s3-secret.yaml` returns a match
    - File has sops metadata: `grep 'encrypted_regex' databases/staging/linkding/linkding-backup-s3-secret.yaml` returns a match
    - No plaintext credentials: `grep 'ACCESS_KEY_ID:' databases/staging/linkding/linkding-backup-s3-secret.yaml` does NOT contain the actual key value in plaintext
  </acceptance_criteria>

  <done>
    databases/staging/linkding/linkding-backup-s3-secret.yaml exists, is SOPS-encrypted, and
    contains `ENC[AES256_GCM` in both stringData fields. The file has the correct name, namespace,
    and type.
  </done>
</task>

<task type="auto">
  <name>Task 3: Patch postgresql-cluster.yaml and activate kustomization</name>
  <files>databases/staging/linkding/postgresql-cluster.yaml, databases/staging/linkding/kustomization.yaml</files>

  <read_first>
    - databases/staging/linkding/postgresql-cluster.yaml (full current content — must preserve all existing fields)
    - databases/staging/linkding/kustomization.yaml (current content — uncomment backup-config.yaml line)
  </read_first>

  <action>
**Step 1 — Patch databases/staging/linkding/postgresql-cluster.yaml**

Add the spec.backup block to the existing Cluster resource. Append it after the spec.bootstrap
block. The full file after the edit must be:

```yaml
apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: linkding-postgres
  namespace: linkding
spec:
  description: "PostgreSQL cluster for Linkding application"
  instances: 1

  # Enable monitoring (remove podMonitorEnabled line)
  # monitoring:
  #   enabled: true
  #   podMonitorEnabled: true

  postgresql:
    parameters:
      max_connections: "100"
      shared_buffers: "128MB"
      effective_cache_size: "512MB"

  storage:
    size: 2Gi
    # storageClass: ""

  superuserSecret:
    name: linkding-superuser

  bootstrap:
    initdb:
      database: linkding
      owner: linkding
      secret:
        name: linkding-db-credentials

  backup:
    barmanObjectStore:
      destinationPath: "s3://<BUCKET_NAME>/linkding"
      endpointURL: "https://<ACCOUNT_ID>.r2.cloudflarestorage.com"
      s3Credentials:
        accessKeyId:
          name: linkding-backup-s3-secret
          key: ACCESS_KEY_ID
        secretAccessKey:
          name: linkding-backup-s3-secret
          key: ACCESS_KEY_SECRET
      wal:
        compression: gzip
      data:
        compression: gzip
    retentionPolicy: "30d"
```

Substitute the actual BUCKET_NAME and ACCOUNT_ID values provided in Task 1 (do not leave
angle-bracket placeholders in the committed file).

CRITICAL — do NOT add a `region:` field. Cloudflare R2 does not require a region and will
reject the request if one is present. AWS S3 users: add `region: us-east-1` (or your bucket
region) under `barmanObjectStore:` if using AWS S3.

The secret key field name is `ACCESS_KEY_SECRET` (matches the key in the SOPS-encrypted secret
created in Task 2). Do not use `ACCESS_SECRET_KEY`.

**Step 2 — Update databases/staging/linkding/kustomization.yaml**

Uncomment `- backup-config.yaml` AND add `- linkding-backup-s3-secret.yaml`. Final file:

```yaml
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: linkding
resources:
  - secrets.yaml
  - postgresql-cluster.yaml
  - backup-config.yaml
  - linkding-backup-s3-secret.yaml
```

Do NOT remove the `namespace: linkding` top-level field — it is required for this kustomization
(unlike the n8n kustomization, which has no top-level namespace).

**Step 3 — Static validation**

```bash
kubectl kustomize databases/staging/linkding/
```

Must exit 0 and output must contain:
- `kind: Cluster` with `barmanObjectStore`
- `kind: ScheduledBackup` (linkding-backup)
- `kind: Secret` with `name: linkding-backup-s3-secret`

```bash
kubectl kustomize databases/staging/linkding/ | grep -c "kind:"
```

Expect at least 4 kinds (Cluster, ScheduledBackup, Secret x2 at minimum — the kustomization
has secrets.yaml which contains two secrets, plus the new one).

**Step 4 — Dry-run validation**

```bash
kubectl apply -k databases/staging/linkding/ --dry-run=client
```

Must exit 0. If it exits non-zero, read the error message carefully:
- "no kind is registered" → kubectl/CNPG CRD version mismatch (rare; not a YAML error)
- "field not allowed" → incorrect field in barmanObjectStore (e.g., spurious `region:`)
- Secret decode errors → unrelated to this change

**Step 5 — Do NOT commit yet** — wait until after Task 4 and Task 5 (n8n) are also complete, then commit all six files together in a single commit. See Task 5 for the final git commit and PR commands.
  </action>

  <verify>
    <automated>kubectl kustomize databases/staging/linkding/ | grep -c "barmanObjectStore"</automated>
  </verify>

  <acceptance_criteria>
    - postgresql-cluster.yaml has barmanObjectStore: `grep "barmanObjectStore" databases/staging/linkding/postgresql-cluster.yaml` returns a match
    - postgresql-cluster.yaml has correct destinationPath: `grep "destinationPath" databases/staging/linkding/postgresql-cluster.yaml` returns a match with a real bucket URL (no angle-bracket placeholders)
    - postgresql-cluster.yaml has correct secret reference: `grep "linkding-backup-s3-secret" databases/staging/linkding/postgresql-cluster.yaml` returns 2 matches (accessKeyId and secretAccessKey)
    - postgresql-cluster.yaml uses ACCESS_KEY_SECRET (not ACCESS_SECRET_KEY): `grep "key: ACCESS_KEY_SECRET" databases/staging/linkding/postgresql-cluster.yaml` returns a match
    - kustomization.yaml includes backup-config.yaml: `grep "^\s*- backup-config.yaml" databases/staging/linkding/kustomization.yaml` returns a match (not commented out)
    - kustomization.yaml includes secret: `grep "linkding-backup-s3-secret.yaml" databases/staging/linkding/kustomization.yaml` returns a match
    - kustomization.yaml preserves top-level namespace: `grep "^namespace: linkding" databases/staging/linkding/kustomization.yaml` returns a match
    - Static build succeeds: `kubectl kustomize databases/staging/linkding/` exits 0
    - Build output includes barmanObjectStore: `kubectl kustomize databases/staging/linkding/ | grep "barmanObjectStore"` returns a match
    - Build output includes ScheduledBackup: `kubectl kustomize databases/staging/linkding/ | grep "kind: ScheduledBackup"` returns a match
    - Dry-run passes: `kubectl apply -k databases/staging/linkding/ --dry-run=client` exits 0
  </acceptance_criteria>

  <done>
    databases/staging/linkding/postgresql-cluster.yaml has spec.backup.barmanObjectStore with a
    real destinationPath (no placeholders) and references linkding-backup-s3-secret for S3 credentials.
    databases/staging/linkding/kustomization.yaml has backup-config.yaml uncommented and
    linkding-backup-s3-secret.yaml added.
    `kubectl kustomize databases/staging/linkding/` exits 0 and includes all resource kinds.
    `kubectl apply -k databases/staging/linkding/ --dry-run=client` exits 0.
  </done>
</task>

<task type="auto">
  <name>Task 4: Create SOPS-encrypted S3 credentials secret for n8n</name>
  <files>databases/staging/n8n/n8n-backup-s3-secret.yaml</files>

  <read_first>
    - databases/staging/n8n/secrets.yaml (reference SOPS format)
    - clusters/staging/.sops.yaml (confirm age recipient)
  </read_first>

  <action>
Credentials are available in `.env` (same bucket, same credentials as linkding — different path prefix).

**Step 1 — Create plaintext secret file**

Write databases/staging/n8n/n8n-backup-s3-secret.yaml:

```yaml
apiVersion: v1
kind: Secret
metadata:
  name: n8n-backup-s3-secret
  namespace: n8n
type: Opaque
stringData:
  ACCESS_KEY_ID: <ACCESS_KEY_ID from .env>
  ACCESS_KEY_SECRET: <ACCESS_KEY_SECRET from .env>
```

**Step 2 — Encrypt in place with SOPS**

```bash
sops --age=age1spwc8lctzldd0ghkkls8jfvzzra7cx95r2zqq6eya84etq65wfgqy2h99p \
  --encrypt \
  --encrypted-regex '^(data|stringData)$' \
  --in-place databases/staging/n8n/n8n-backup-s3-secret.yaml
```

**Step 3 — Verify encryption**

```bash
grep -c 'ENC\[AES256_GCM' databases/staging/n8n/n8n-backup-s3-secret.yaml
```

Must return 2. Never commit if it contains plaintext credential values.
  </action>

  <verify>
    <automated>grep -c 'ENC\[AES256_GCM' databases/staging/n8n/n8n-backup-s3-secret.yaml</automated>
  </verify>

  <acceptance_criteria>
    - File exists: `ls databases/staging/n8n/n8n-backup-s3-secret.yaml` exits 0
    - File is encrypted: `grep -c 'ENC\[AES256_GCM' databases/staging/n8n/n8n-backup-s3-secret.yaml` returns 2
    - File has correct name: `grep 'name: n8n-backup-s3-secret' databases/staging/n8n/n8n-backup-s3-secret.yaml` returns a match
    - File has correct namespace: `grep 'namespace: n8n' databases/staging/n8n/n8n-backup-s3-secret.yaml` returns a match
    - File has sops metadata: `grep 'encrypted_regex' databases/staging/n8n/n8n-backup-s3-secret.yaml` returns a match
  </acceptance_criteria>

  <done>
    databases/staging/n8n/n8n-backup-s3-secret.yaml exists, is SOPS-encrypted, and
    contains `ENC[AES256_GCM` in both stringData fields.
  </done>
</task>

<task type="auto">
  <name>Task 5: Patch n8n postgresql-cluster.yaml and kustomization, then commit all and open PR</name>
  <files>databases/staging/n8n/postgresql-cluster.yaml, databases/staging/n8n/kustomization.yaml</files>

  <read_first>
    - databases/staging/n8n/postgresql-cluster.yaml (full current content — must preserve all existing fields)
    - databases/staging/n8n/kustomization.yaml (current content)
    - .env (for BUCKET_NAME and ACCOUNT_ID values)
  </read_first>

  <action>
**Step 1 — Patch databases/staging/n8n/postgresql-cluster.yaml**

Add spec.backup block after spec.bootstrap. Final file:

```yaml
apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: n8n-postgresql-cluster
  namespace: n8n
spec:
  description: "PostgreSQL cluster for n8n application"
  instances: 1

  # Enable monitoring (remove podMonitorEnabled line)
  # monitoring:
  #   enabled: true
  #   podMonitorEnabled: true

  postgresql:
    parameters:
      max_connections: "200"
      shared_buffers: "128MB"
      effective_cache_size: "512MB"

  storage:
    size: 2Gi
    # storageClass: "" # Use default storage class

  superuserSecret:
    name: n8n-superuser

  bootstrap:
    initdb:
      database: n8n
      owner: n8n
      secret:
        name: n8n-db-credentials

  backup:
    barmanObjectStore:
      destinationPath: "s3://<BUCKET_NAME>/n8n"
      endpointURL: "https://<ACCOUNT_ID>.r2.cloudflarestorage.com"
      s3Credentials:
        accessKeyId:
          name: n8n-backup-s3-secret
          key: ACCESS_KEY_ID
        secretAccessKey:
          name: n8n-backup-s3-secret
          key: ACCESS_KEY_SECRET
      wal:
        compression: gzip
      data:
        compression: gzip
    retentionPolicy: "30d"
```

Substitute real BUCKET_NAME (`homelab-postgres-backups`) and ACCOUNT_ID from `.env`.
Do NOT add a `region:` field (Cloudflare R2 does not require it).

**Step 2 — Update databases/staging/n8n/kustomization.yaml**

Add `- n8n-backup-s3-secret.yaml` (backup-config.yaml is already active). Final file:

```yaml
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - secrets.yaml
  - postgresql-cluster.yaml
  - backup-config.yaml
  - n8n-backup-s3-secret.yaml
```

NOTE: n8n kustomization has NO top-level `namespace:` field — do not add one.

**Step 3 — Static validation for n8n**

```bash
kubectl kustomize databases/staging/n8n/
kubectl apply -k databases/staging/n8n/ --dry-run=client
```

Both must exit 0. Build output must include `barmanObjectStore` and `kind: ScheduledBackup`.

**Step 4 — Commit all six files and open PR**

```bash
git add databases/staging/linkding/postgresql-cluster.yaml \
        databases/staging/linkding/kustomization.yaml \
        databases/staging/linkding/linkding-backup-s3-secret.yaml \
        databases/staging/n8n/postgresql-cluster.yaml \
        databases/staging/n8n/kustomization.yaml \
        databases/staging/n8n/n8n-backup-s3-secret.yaml

git commit -m "feat(databases): add barmanObjectStore S3 backup destination to linkding and n8n clusters

- Add spec.backup.barmanObjectStore to linkding-postgres pointing at Cloudflare R2
- Add spec.backup.barmanObjectStore to n8n-postgresql-cluster pointing at Cloudflare R2
- Create SOPS-encrypted linkding-backup-s3-secret and n8n-backup-s3-secret
- Activate linkding backup-config.yaml and add both secrets to kustomizations
- Both clusters share homelab-postgres-backups bucket with separate path prefixes

Closes BACK-02"

gh pr create \
  --base main \
  --head feat/phase-5-fix-postgres-backup-destinations \
  --title "feat(databases): add S3 backup destination to linkding and n8n PostgreSQL clusters" \
  --body "$(cat <<'EOF'
## Summary

- Adds `spec.backup.barmanObjectStore` to both `linkding-postgres` and `n8n-postgresql-cluster` Clusters (Cloudflare R2 destination)
- Creates SOPS-encrypted S3 credential secrets for both namespaces
- Activates `backup-config.yaml` for linkding (already active for n8n)
- Both clusters use the `homelab-postgres-backups` R2 bucket with separate prefixes (`/linkding`, `/n8n`)

## Validation

- `kubectl kustomize databases/staging/linkding/` exits 0 ✓
- `kubectl kustomize databases/staging/n8n/` exits 0 ✓
- `kubectl apply -k databases/staging/linkding/ --dry-run=client` exits 0 ✓
- `kubectl apply -k databases/staging/n8n/ --dry-run=client` exits 0 ✓
- All secrets SOPS-encrypted (ENC[AES256_GCM in stringData) ✓

## Post-Merge

After FluxCD syncs (~1 min), verify for each namespace:
- `kubectl get scheduledbackup -n linkding` and `kubectl get scheduledbackup -n n8n`
- `kubectl get backup -n linkding` and `kubectl get backup -n n8n` show completed backups
- WAL archiving returns `True` for both clusters

Closes BACK-02
EOF
)"
```
  </action>

  <verify>
    <automated>kubectl kustomize databases/staging/n8n/ | grep -c "barmanObjectStore"</automated>
  </verify>

  <acceptance_criteria>
    - n8n postgresql-cluster.yaml has barmanObjectStore: `grep "barmanObjectStore" databases/staging/n8n/postgresql-cluster.yaml` returns a match
    - n8n postgresql-cluster.yaml destinationPath ends with /n8n: `grep "destinationPath.*\/n8n" databases/staging/n8n/postgresql-cluster.yaml` returns a match
    - n8n postgresql-cluster.yaml references n8n-backup-s3-secret: `grep "n8n-backup-s3-secret" databases/staging/n8n/postgresql-cluster.yaml` returns 2 matches
    - n8n kustomization includes n8n-backup-s3-secret.yaml: `grep "n8n-backup-s3-secret.yaml" databases/staging/n8n/kustomization.yaml` returns a match
    - n8n kustomization has no top-level namespace field: `grep "^namespace:" databases/staging/n8n/kustomization.yaml` returns no match
    - n8n static build succeeds: `kubectl kustomize databases/staging/n8n/` exits 0
    - n8n dry-run passes: `kubectl apply -k databases/staging/n8n/ --dry-run=client` exits 0
    - PR is open: `gh pr list --head feat/phase-5-fix-postgres-backup-destinations` shows one PR
  </acceptance_criteria>

  <done>
    Both clusters have spec.backup.barmanObjectStore with real destinationPaths.
    Both kustomizations include their respective secrets.
    Both static builds and dry-runs pass.
    A single PR is open targeting main with all six files changed.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify backups complete successfully after PR merge</name>
  <files>none</files>
  <action>Human merges the PR and verifies backups actually complete for both linkding and n8n.</action>
  <verify>
    <automated>kubectl get backup -n linkding && kubectl get backup -n n8n</automated>
  </verify>
  <done>
    At least one Backup object in each namespace (linkding, n8n) shows `status.phase: completed`.
    WAL archiving condition returns True for both clusters. S3 bucket contains backup files under
    both `/linkding` and `/n8n` prefixes.
  </done>
  <what-built>
    Six files changed: both postgresql-cluster.yaml files (barmanObjectStore added), both
    kustomization.yaml files (secrets added, linkding backup-config.yaml uncommented),
    linkding-backup-s3-secret.yaml and n8n-backup-s3-secret.yaml (new, SOPS-encrypted).
    A PR is open targeting main.
  </what-built>
  <how-to-verify>
    1. Review the PR diff — confirm six files changed, no plaintext credentials visible.

    2. Merge the PR to main.

    3. Wait for FluxCD to sync (~1 minute):
       ```bash
       flux get kustomization databases
       ```

    4. Check pods restarted for both clusters (expected — CNPG reconfigures archive_command):
       ```bash
       kubectl get pods -n linkding && kubectl get pods -n n8n
       ```

    5. Verify ScheduledBackups are active:
       ```bash
       kubectl get scheduledbackup -n linkding
       kubectl get scheduledbackup -n n8n
       ```

    6. Check for completed backups (`immediate: true` fires one on creation):
       ```bash
       kubectl get backup -n linkding
       kubectl get backup -n n8n
       ```
       Expected: At least one Backup with `status: completed` in each namespace.

    7. If backup fails with "NoSuchBucket": create the bucket in R2, then:
       ```bash
       kubectl annotate cluster linkding-postgres -n linkding cnpg.io/immediateBackup="true" --overwrite
       kubectl annotate cluster n8n-postgresql-cluster -n n8n cnpg.io/immediateBackup="true" --overwrite
       ```

    8. Verify WAL archiving:
       ```bash
       kubectl get cluster -n linkding linkding-postgres \
         -o jsonpath='{.status.conditions[?(@.type=="ContinuousArchiving")].status}'
       kubectl get cluster -n n8n n8n-postgresql-cluster \
         -o jsonpath='{.status.conditions[?(@.type=="ContinuousArchiving")].status}'
       ```
       Expected: `True` for both.

    9. Check R2 bucket — expect both `linkding/` and `n8n/` prefixes with WAL files.
  </how-to-verify>
  <resume-signal>Type "approved" to complete the phase, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
Phase verification commands (run after FluxCD sync):

```bash
# linkding
kubectl get scheduledbackup -n linkding
kubectl get backup -n linkding
kubectl get cluster -n linkding linkding-postgres \
  -o jsonpath='{.status.conditions[?(@.type=="ContinuousArchiving")].status}'
kubectl kustomize databases/staging/linkding/ | grep -A5 "barmanObjectStore"

# n8n
kubectl get scheduledbackup -n n8n
kubectl get backup -n n8n
kubectl get cluster -n n8n n8n-postgresql-cluster \
  -o jsonpath='{.status.conditions[?(@.type=="ContinuousArchiving")].status}'
kubectl kustomize databases/staging/n8n/ | grep -A5 "barmanObjectStore"
```

**Expected pod behavior on first sync:** Both postgres pods will restart once when
barmanObjectStore is added. This is normal — CNPG reconfigures archive_command. ~10-30 seconds.
</verification>

<success_criteria>
Phase 5 is complete when:
- `kubectl get backup -n linkding` shows at least one Backup with `status.phase: completed`
- `kubectl get backup -n n8n` shows at least one Backup with `status.phase: completed`
- WAL archiving returns `True` for both clusters
- The PR is merged to main and FluxCD has synced (flux get kustomization databases shows healthy)
- No "no barmanObjectStore section defined" error in either namespace
- R2 bucket contains both `linkding/` and `n8n/` prefixes with backup files
</success_criteria>

<output>
After completion, create `.planning/phases/05-fix-linkding-backup-destination/05-01-SUMMARY.md`
using the summary template at `@$HOME/.claude/get-shit-done/templates/summary.md`.
</output>
