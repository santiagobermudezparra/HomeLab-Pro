apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: forgejo-postgres
  namespace: forgejo
spec:
  description: "PostgreSQL cluster for Forgejo application"
  instances: 1

  # monitoring:
  #   enabled: true
  #   podMonitorEnabled: true

  postgresql:
    parameters:
      max_connections: "100"
      shared_buffers: "128MB"
      effective_cache_size: "512MB"

  storage:
    size: 2Gi
    pvcTemplate:
      storageClassName: longhorn
      accessModes:
        - ReadWriteOnce
      resources:
        requests:
          storage: 2Gi

  superuserSecret:
    name: forgejo-superuser

  bootstrap:
    initdb:
      database: forgejo
      owner: forgejo
      secret:
        name: forgejo-db-credentials

  backup:
    barmanObjectStore:
      destinationPath: "s3://homelab-postgres-backup/forgejo"
      endpointURL: "https://cf504e28de7836d9611b6774cdcb303e.r2.cloudflarestorage.com"
      s3Credentials:
        accessKeyId:
          name: forgejo-backup-s3-secret
          key: ACCESS_KEY_ID
        secretAccessKey:
          name: forgejo-backup-s3-secret
          key: ACCESS_KEY_SECRET
      wal:
        compression: gzip
      data:
        compression: gzip
    retentionPolicy: "7d"
